import * as React from "react"
import Layout from '../components/Layout'

// markup
const NotFoundPage = () => {
  return (
    <Layout>
      <div>404. It's empty in here. No such page.</div>
    </Layout>
  )
}

export default NotFoundPage
